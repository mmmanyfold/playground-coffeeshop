(ns playground-coffeeshop.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [playground-coffeeshop.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
